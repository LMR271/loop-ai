<p>Hi,</p>

<p><strong><%= @organization_name %></strong> wants your feedback.
  <% if @loop.description.present? %>
    <%= @loop.description %>
  <% end %>
</p>

<%= render "mailer/button", text: "Share your feedback", url: @respondent_url %>

<p>It's a short voice conversation, not a form — just talk naturally.</p>
